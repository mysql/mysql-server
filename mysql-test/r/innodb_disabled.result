CALL mtr.add_suppression("The use of InnoDB is mandatory since MySQL 5\.7\. The former options like .* are ignored\.");
SELECT support FROM INFORMATION_SCHEMA.ENGINES WHERE engine = 'innodb';
support
DEFAULT
