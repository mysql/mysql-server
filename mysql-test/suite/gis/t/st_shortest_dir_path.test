create table paths(
    id int primary key,
    from_id int,
    to_id int,
    cost double
);

insert into paths values
    (0, 0, 1, 5.0),
    (1, 1, 2, 5.0),
    (2, 0, 2, 15.0),
    (3, 1, 3, 2.0),
    (4, 3, 2, 2.0)
    ;

select st_shortest_dir_path(id, from_id, to_id, cost, id, 2) from paths;

drop table paths;
