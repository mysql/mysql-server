--echo #
--echo # WL#6965: Truncate UNDO logs during recovery.
--echo # Crash after deleting the old undo tablespace during truncate operation 
--echo #

let CRASH_ON_SHUTDOWN = 1;
let CRASH_POINT = ib_undo_trunc_after_delete_undo_tablespace;
let USE_EXPLICIT = 0;

--source suite/innodb_undo/include/truncate_at_shutdown.inc
