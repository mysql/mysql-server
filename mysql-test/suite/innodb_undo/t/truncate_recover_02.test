--echo #
--echo # WL#6965: Truncate UNDO logs during recovery.
--echo # Crash before writing the undo unusable flag
--echo #

let CRASH_ON_SHUTDOWN = 1;
let CRASH_POINT = ib_undo_trunc_before_writing_unusable_flag;
let USE_EXPLICIT = 0;

--source suite/innodb_undo/include/truncate_at_shutdown.inc
