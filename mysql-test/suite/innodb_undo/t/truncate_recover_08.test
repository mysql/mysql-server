--echo #
--echo # WL#6965: Truncate UNDO logs during recovery.
--echo # Crash after updating dd and unsetting the unusable flag in the undo  
--echo # tablespace
--echo #

let CRASH_ON_SHUTDOWN = 1;
let CRASH_POINT = ib_undo_trunc_after_unsetting_the_flag;
let USE_EXPLICIT = 0;

--source suite/innodb_undo/include/truncate_at_shutdown.inc
