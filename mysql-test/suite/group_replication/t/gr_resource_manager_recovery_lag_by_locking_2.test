###############################################################################
# This test verifies that a server reported as lagging during recovery by
# the component group_replication_resource_manager, reports no recovery lag
# once recovery is complete.
#
# 0. The test requires two servers.
# 1. Install component group_replication_resource_manager and
#    start group replication on all members.
#    server1 will be the primary.
#    Create the table t1 in the group.
# 2. Assert that there is no lag on server2.
# 3. Remove server2 from the group.
# 4. Write a row in table t1 on the primary (server1).
# 5. Lock table t3 to block future writes on it on server2.
#    Add server 2 back to the group.
# 6. Since table t1 is locked, the recovery channel will
#    lag, roughly after 50 seconds server2 will try to be
#    evicted, which will not happen since the group only
#    two members.
# 7. Unlock table t1, recovery will complete.
#    Assert that lag decreases to 0 on server2.
# 8. Cleanup

--source include/have_group_replication_resource_manager.inc
--source include/big_test.inc
--source include/have_debug.inc
--source include/have_group_replication_plugin.inc
--let $rpl_skip_group_replication_start= 1
--let $rpl_group_replication_single_primary_mode=1
--source include/group_replication.inc


--echo
--echo #########################################################
--echo # 1. Install component group_replication_resource_manager and
--echo #    start group replication on all members.
--echo #    server1 will be the primary.
--echo #    Create the table t1 in the group.
--let $rpl_connection_name= server1
--source include/connection.inc

INSTALL COMPONENT 'file://component_group_replication_resource_manager';
--source include/start_and_bootstrap_group_replication.inc
CREATE TABLE t1 (c1 INT NOT NULL PRIMARY KEY);

--let $rpl_connection_name= server2
--source include/connection.inc

SET SESSION sql_log_bin= 0;
call mtr.add_suppression("Member threshold exceeded: recovery channel lag.*. However, the group has less than 3 members, the member will not leave the group.");
SET SESSION sql_log_bin= 1;

INSTALL COMPONENT 'file://component_group_replication_resource_manager';
--source include/start_group_replication.inc


--echo
--echo #########################################################
--echo # 2. Assert that there is no lag on server 2.
--let $rpl_connection_name= server2
--source include/connection.inc

--let $assert_text= 'Eviction timestamp is not set.'
--let $assert_cond= [SELECT COUNT(*) AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_eviction_timestamp" AND VARIABLE_VALUE = "", COUNT, 1] = 1
--source include/assert.inc

--let $assert_text= 'There are no recovery channel threshold hits.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_threshold_hits", VARIABLE_VALUE, 1] = 0
--source include/assert.inc

--let $assert_text= 'There is no lag.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_lag", VARIABLE_VALUE, 1] = 0
--source include/assert.inc


--echo
--echo #########################################################
--echo # 3. Remove server2 from the group.
--let $rpl_connection_name= server2
--source include/connection.inc
--source include/stop_group_replication.inc


--echo
--echo #########################################################
--echo # 4. Write a row in table t1 on the primary (server1).
--let $rpl_connection_name= server1
--source include/connection.inc
INSERT INTO t1 VALUES (1);


--echo
--echo #########################################################
--echo # 5. Lock table t3 to block future writes on it on server2.
--echo #    Add server2 back to the group.
--let $rpl_connection_name= server2
--source include/connection.inc
SET GLOBAL group_replication_resource_manager.recovery_channel_lag = 1;

--let $rpl_connection_name= server_2
--source include/connection.inc
LOCK TABLE t1 WRITE;

--let $rpl_connection_name= server2
--source include/connection.inc
SET GLOBAL debug= '+d,group_replication_skip_read_mode';
--let $group_replication_start_member_state= RECOVERING
--source include/start_group_replication.inc


--echo
--echo #########################################################
--echo # 6. Since table t1 is locked, the recovery channel will
--echo #    lag, roughly after 50 seconds server2 will try to be
--echo #    evicted, which will not happen since the group only
--echo #    two members.
--let $rpl_connection_name= server2
--source include/connection.inc

--echo 'There are recovery channel threshold hits.'
--let $wait_condition= SELECT COUNT(*)=1 AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_threshold_hits" AND VARIABLE_VALUE >= 11
--source include/wait_condition.inc

--let $assert_text= 'Eviction timestamp is not set.'
--let $assert_cond= [SELECT COUNT(*) AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_eviction_timestamp" AND VARIABLE_VALUE = "", COUNT, 1] = 1
--source include/assert.inc

--let $assert_text= 'There is lag.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_lag", VARIABLE_VALUE, 1] >= 50
--source include/assert.inc


--echo
--echo #########################################################
--echo # 7. Unlock table t1, recovery will complete.
--echo #    Assert that lag decreases to 0 on server2.
--let $rpl_connection_name= server_2
--source include/connection.inc
UNLOCK TABLES;

--let $rpl_connection_name= server2
--source include/connection.inc
--let $group_replication_member_state= ONLINE
--source include/gr_wait_for_member_state.inc

--echo 'There is no lag.'
--let $wait_condition= SELECT COUNT(*)=1 AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_lag" AND VARIABLE_VALUE = 0
--source include/wait_condition.inc

--let $assert_text= 'Eviction timestamp is not set.'
--let $assert_cond= [SELECT COUNT(*) AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_eviction_timestamp" AND VARIABLE_VALUE = "", COUNT, 1] = 1
--source include/assert.inc

--let $assert_text= 'There are recovery channel threshold hits.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_threshold_hits", VARIABLE_VALUE, 1] >= 11
--source include/assert.inc


--echo ############################################################
--echo # 8. Cleanup
--let $rpl_connection_name= server1
--source include/connection.inc
DROP TABLE t1;
--source include/rpl/sync.inc

--let $rpl_connection_name= server2
--source include/connection.inc
--let $skip_assert_read_only= 1
--source include/stop_group_replication.inc
SET GLOBAL debug= '-d,group_replication_skip_read_mode';
UNINSTALL COMPONENT 'file://component_group_replication_resource_manager';

--let $rpl_connection_name= server1
--source include/connection.inc
--source include/stop_group_replication.inc
UNINSTALL COMPONENT 'file://component_group_replication_resource_manager';

--source include/group_replication_end.inc
