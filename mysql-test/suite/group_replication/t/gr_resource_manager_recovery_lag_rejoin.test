###############################################################################
# This test verifies that the component
# group_replication_resource_manager remove the member having recovery
# lag from the group and member rejoins the group.
#
# 0. The test requires three servers: M1 , M2 and M3
# 1. Install component group_replication_resource_manager on M3.
#    Start group replication on M1, M2 and M3.
# 2. Assert Gr_resource_manager_recovery_channel_eviction_timestamp
#    and Gr_resource_manager_recovery_channel_threshold_hits
#    are not set on M3.
# 3. Set recovery_channel_lag to 0.
#    Set debug point on M3 to return lag.
#    M3 is ONLINE. Since recovery_channel_lag = 0, disables the feature.
# 4. Set applier_channel_lag to 1 and quarantine_time=3600.
#    Secondary will not leave the group since it joined
#    less than 1 hour ago.
# 5. Set recovery_channel_lag to 1 and quarantine_time=0.
#    M3 will leave the group and rejoin.
# 6. Assert Gr_resource_manager_recovery_channel_eviction_timestamp
#    and Gr_resource_manager_recovery_channel_threshold_hits
#    are set. Lag will be 1000 or 0.
# 7. Cleanup

--source include/have_group_replication_resource_manager.inc
--source include/big_test.inc
--source include/have_debug.inc
--source include/have_group_replication_plugin.inc
--let $rpl_server_count= 3
--let $rpl_skip_group_replication_start= 1
--let $rpl_group_replication_single_primary_mode=1
--source include/group_replication.inc

--echo
--echo #########################################################
--echo # 1. Install component group_replication_resource_manager on M3.
--echo #    Start group replication on M1, M2 and M3.

--let $rpl_connection_name= server1
--source include/connection.inc

--source include/start_and_bootstrap_group_replication.inc

--let $rpl_connection_name= server2
--source include/connection.inc

--source include/start_group_replication.inc

--let $rpl_connection_name= server3
--source include/connection.inc

--source include/gr_autorejoin_monitoring.inc

SET SESSION sql_log_bin= 0;
call mtr.add_suppression("The server was automatically set into read only mode after an error was detected.");
call mtr.add_suppression("The server was automatically set into offline mode after an error was detected.");
call mtr.add_suppression("Member exiting group: recovery channel lag.*");
call mtr.add_suppression("Member threshold exceeded: recovery channel lag of 1000 seconds exceeded tolerance threshold configured to 1 seconds. However, since it joined .* seconds ago and the quarantine period is configured to 3600 seconds, the member will not leave the group.");
SET SESSION sql_log_bin= 1;

INSTALL COMPONENT 'file://component_group_replication_resource_manager';
SET @quarantine_time= @@global.group_replication_resource_manager.quarantine_time;
SET GLOBAL group_replication_resource_manager.quarantine_time = 0;
--source include/start_group_replication.inc

--echo
--echo #########################################################
--echo # 2. Assert Gr_resource_manager_recovery_channel_eviction_timestamp
--echo #    and Gr_resource_manager_recovery_channel_threshold_hits
--echo #    are not set on M3.

--let $rpl_connection_name= server3
--source include/connection.inc

--let $assert_text= 'Eviction timestamp is not set.'
--let $assert_cond= [SELECT COUNT(*) AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_eviction_timestamp" AND VARIABLE_VALUE = "", COUNT, 1] = 1
--source include/assert.inc

--let $assert_text= 'There are no recovery channel threshold hits.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_threshold_hits", VARIABLE_VALUE, 1] = 0
--source include/assert.inc

--let $assert_text= 'Lag is 0.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_lag", VARIABLE_VALUE, 1] = 0
--source include/assert.inc

--echo
--echo #########################################################
--echo # 3. Set recovery_channel_lag to 0.
--echo #    Set debug point on M3 to return lag.
--echo #    M3 is ONLINE. Since recovery_channel_lag = 0, disables the feature.

SET @channel_lag= @@global.group_replication_resource_manager.recovery_channel_lag;
SET @@GLOBAL.group_replication_resource_manager.recovery_channel_lag=0;
SET @@GLOBAL.DEBUG='+d,group_replication_stop_before_rejoin_loop';
SET @@GLOBAL.DEBUG= '+d,GR_RM_recovery_force_lag';

# Wait for 10 samples.
--sleep 60

--let $group_replication_member_state= ONLINE
--source include/gr_wait_for_member_state.inc

--echo
--echo #########################################################
--echo # 4. Set applier_channel_lag to 1 and quarantine_time=3600.
--echo #    Secondary will not leave the group since it joined
--echo #    less than 1 hour ago.

SET GLOBAL group_replication_resource_manager.quarantine_time = 3600;
SET @@GLOBAL.group_replication_resource_manager.recovery_channel_lag=1;

# Wait for 10 samples.
--sleep 60

--let $group_replication_member_state= ONLINE
--source include/gr_wait_for_member_state.inc

--echo
--echo #########################################################
--echo # 5. Set recovery_channel_lag to 1 and quarantine_time=0.
--echo #    M3 will leave the group and rejoin.

SET GLOBAL group_replication_resource_manager.quarantine_time = 0;
SET @@GLOBAL.group_replication_resource_manager.recovery_channel_lag=1;

SET DEBUG_SYNC = "now WAIT_FOR signal.autorejoin_entering_loop";
SET @@GLOBAL.DEBUG='-d,group_replication_stop_before_rejoin_loop';
SET @@GLOBAL.DEBUG= '-d,GR_RM_recovery_force_lag';

# Verify that it is currently running
--let $assert_text = Auto-rejoin should be running
--let $assert_cond = [SELECT IS_AUTOREJOIN_RUNNING()] = TRUE
--source include/assert.inc

SET DEBUG_SYNC = "now SIGNAL signal.autorejoin_enter_loop";

--let $group_replication_member_state= ONLINE
--source include/gr_wait_for_member_state.inc

--echo
--echo #########################################################
--echo # 6. Assert Gr_resource_manager_recovery_channel_eviction_timestamp
--echo #    and Gr_resource_manager_recovery_channel_threshold_hits
--echo #    are set. Lag will be 1000 or 0.

--echo 'Eviction timestamp is set.'
--let $wait_condition= SELECT COUNT(*)=1 AS COUNT FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_eviction_timestamp" AND VARIABLE_VALUE <> ""
--source include/wait_condition.inc

--let $assert_text= 'There are recovery channel threshold hits.'
--let $assert_cond= [SELECT VARIABLE_VALUE FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_threshold_hits", VARIABLE_VALUE, 1] >= 10
--source include/assert.inc

# The lag will be 0 on the lag information fetch after the member
# left the group.
--let $assert_text= 'Lag is 1000 or 0.'
--let $assert_cond= [SELECT COUNT(*) count FROM performance_schema.global_status WHERE VARIABLE_NAME="Gr_resource_manager_recovery_channel_lag" AND (VARIABLE_VALUE=0 OR VARIABLE_VALUE=1000), count, 1] = 1
--source include/assert.inc
SET @@GLOBAL.DEBUG= '-d,GR_RM_recovery_force_lag';

--echo ############################################################
--echo # 7. Cleanup

--let $rpl_connection_name= server3
--source include/connection.inc
--source include/stop_group_replication.inc

SET @@global.group_replication_resource_manager.recovery_channel_lag = @channel_lag;
SET @@global.group_replication_resource_manager.quarantine_time = @quarantine_time;

UNINSTALL COMPONENT 'file://component_group_replication_resource_manager';
--source include/gr_end_autorejoin_monitoring.inc
--source include/group_replication_end.inc
