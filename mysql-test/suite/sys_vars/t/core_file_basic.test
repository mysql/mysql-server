# Performance Schema is not supported by embedded server. Hence skip when test runs with embedded mode.
-- source include/not_embedded.inc
--disable_warnings
SELECT * FROM performance_schema.global_variables WHERE VARIABLE_NAME LIKE '%core%';
--enable_warnings
SHOW GLOBAL VARIABLES LIKE '%core%';
