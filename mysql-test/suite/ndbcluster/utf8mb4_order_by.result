create table t (c varchar(20),d varchar(20)) engine=ndb;
insert into t values ('\b','backspace');
select sum(char_length(c)) from t;
sum(char_length(c))
1
insert into t values (X'10','data link escape');
select sum(char_length(c)) from t;
sum(char_length(c))
2
select * from t order by c;
c	d
	backspace
	data link escape
drop table t;
