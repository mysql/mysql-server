# Test remote clone with different table types with debug sync
--source include/have_punch_hole.inc

--let $HOST = 127.0.0.1
--let $PORT =`select @@port`
--let $USER = root
--let remote_clone = 1

--source local_compress.test
