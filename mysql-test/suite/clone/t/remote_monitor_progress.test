# Monitor remote clone operations using performance schema's stage and statement events.

--let $HOST = 127.0.0.1
--let $PORT =`select @@port`
--let $USER = root
--let remote_clone = 1

--source monitor_progress.test
