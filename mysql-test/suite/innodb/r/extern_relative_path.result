#
# Make sure that a relative path in --innodb-directories will work,
# even if that path does not exist at startup.
#
CREATE TABLESPACE ts1 ADD DATAFILE 'MYSQLTEST_VARDIR/Extern_Data/ts1.ibd';
DROP TABLESPACE ts1;
