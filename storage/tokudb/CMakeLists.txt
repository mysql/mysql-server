SET(TOKUDB_VERSION $ENV{TOKUDB_VERSION})
SET(TOKUFRACTALTREE $ENV{TOKUFRACTALTREE})
SET(TOKUFRACTALTREE_LIBNAME $ENV{TOKUFRACTALTREE_LIBNAME})

ADD_DEFINITIONS("-DTOKUDB_VERSION=\"${TOKUDB_VERSION}\"")
INCLUDE_DIRECTORIES(${TOKUFRACTALTREE}/include)

SET(TOKUDB_PLUGIN_DYNAMIC "ha_tokudb")
SET(TOKUDB_SOURCES hatoku_hton.cc hatoku_cmp.cc ha_tokudb.cc)
MYSQL_ADD_PLUGIN(tokudb ${TOKUDB_SOURCES} STORAGE_ENGINE MODULE_ONLY LINK_LIBRARIES "-L${TOKUFRACTALTREE}/lib" ${TOKUFRACTALTREE_LIBNAME} tokuportability)
